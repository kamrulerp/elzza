
<img 
    src="{{ setting('logo') ?? 'https://placehold.co/32x32?text=Logo' }}" 
    alt="{{ setting('site_name') ?? 'Company Logo' }}" 
    class="{{ setting('logo') ? 'brand-image opacity-75 shadow' : 'w-20 h-20' }}"
/>
